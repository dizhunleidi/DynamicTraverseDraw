# 更新日志

## v3.0 - 2026-09-04

v3.0 相比旧版本是一次完整重构，接口、目录组织和接入方式均以当前代码为准，旧版本的实现和使用方式不再作为兼容目标。

- 完成框架整体重构，重新划分 `core`、`games`、`diRW` 和 UI 壳的职责。
- 新增 `GameRuntime`，支持游戏实例初始化、停止和运行时切换。
- 新增模块机制，统一绘图、自瞄、调试页面和其他游戏功能的生命周期。
- 将对象管理改为模板化 `TrackedList<T>`，加入地址索引、墓碑回收和更新回调。
- 将大型对象数组遍历抽象为 `FrameScanner` 分帧批处理。
- 重写向量、投影和通用屏幕绘制辅助接口。
- 增加 `texun.hpp` 完整实例、`Example.hpp` 教学示例和 `GameTemplate.hpp` 空白骨架。
- 补充框架文档、README 和 GitHub 发布结构。

